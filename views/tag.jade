extends layout

block content
  .row
    .span2
      include includes/blogNavigation
    .span6
      h3
        a(href=tagUrl(tag)) posts tagged * #{tag}
      - each post in posts
        include includes/postSnippet
